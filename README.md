# aa-recorder
A self-contained desktop app that opens an isolated Chromium window, records **only** GA4 network requests until the user stops, displays them live in a timeline, and exports them to CSV (1 event per row, with sequence numbers and timestamps). It can also reload a CSV to review the journey in-app.
